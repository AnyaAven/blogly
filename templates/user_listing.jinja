{% extends 'base.jinja' %}
{% block content %}

  <h1>Users</h1>

  <ul>
  {# FIXME: are we passing users correctly? #}
    {% for user in user_full_names %}
      <li><a href="/Users/{{ user_ids }}">{{ user }}</a></li> {# FIXME: don't pass in just user_ids the array #}
    {% endfor %}
  </ul>

    <a href="/users/new">
        <button class="btn btn-primary btn-lg w-100">
            Add user
        </button>
    </a>

  </form>

{% endblock %}


