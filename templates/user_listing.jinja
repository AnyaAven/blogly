{% extends 'base.jinja' %}
{% block content %}

    <!-- Test comment for user_listing -->
    <h1>Users</h1>

    <ul>
      {% for user_id in user_mapping %} {# FIXME: for user in users #}

        {# FIXME: user.id    and    user.get_full_name #}
        <li><a href="/users/{{ user_id }}">{{ user_mapping[user_id] }}</a></li>
      {% endfor %}
    </ul>


    <a href="/users/new" class="btn btn-primary btn-lg w-100">
        Add user
    </a>


{% endblock %}


